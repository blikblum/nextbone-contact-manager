{
  "compilerOptions": {
    // This must be specified if "paths" is set
    "experimentalDecorators": true,
    "baseUrl": ".",
    // Relative to "baseUrl"
    "paths": {
      "*": [
        "*",
        "./src/common/*"
      ]
    }
  }
}
